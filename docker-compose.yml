services:
  frontend:
    image: 310297108749.dkr.ecr.ap-south-1.amazonaws.com/ortho/appointment:latest
    container_name: ortho_frontend
    restart: always
    ports:
      - "80:80"
      - "443:443"
    environment:
      BACKEND_PRIVATE_IP: "10.0.2.194"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
